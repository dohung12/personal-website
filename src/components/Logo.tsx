import logo from '../assets/logo.svg';

const Logo = () => {
  return (
    <img
      src={logo}
      alt='logo'
      style={{
        height: '4rem',
      }}
    />
  );
};

export default Logo;
